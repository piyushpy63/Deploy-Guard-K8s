apiVersion: rbac.authorization.k8s.io/v1

kind: ClusterRoleBinding

metadata:
  name: deploy-guard

subjects: 
  - kind: ServiceAccount
    name: deploy-guard
    namespace: deploy-guard


roleRef:
  kind: ClusterRole
  name: deploy-guard
  apiGroup: rbac.authorization.k8s.io


